package progress

// The easiest way to get a progressbar is as follows
func Example() {
	var p Progress
	p.GetBar(275, 346)
}

// The easiest way to get a progressbar is as follows
func Example_styled() {
	var p Progress
	p.SetStyle("")
	p.GetBar(275, 346)
}

// The easiest way to get a progressbar is as follows
func Example() {
	var p Progress
	p.GetBar(275, 346)
}
